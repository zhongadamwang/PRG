# Usage Example:

**Query with Entity and Feature:**
> Use service-generator skill to create a service architecture shell for the Request feature page with IRequestRepository dependency injection.
**带实体和特性的提问：**
> 使用 service-generator skill，为 Request 特性页面创建服务架构外壳，包含 IRequestRepository 依赖注入。
